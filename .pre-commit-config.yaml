repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: local
    hooks:
      - id: tox
        name: tox
        entry: bash -c "cd ambient-weather-ha && uv run tox"
        language: system
        pass_filenames: false
        always_run: true
